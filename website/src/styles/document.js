import config from '../config'

const styles = theme => {
  return {
    pageHeader: {
      fontWeight: 'bold',
      color: theme.palette.primary.main,
      padding: '12px',
      paddingLeft: '0px',
    },
    button: {
      margin: '10px',
    }
  }
}

export default styles
